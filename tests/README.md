# Tests

- `unit/`: correctness tests for embedded-policy reading, verification, and authorization logic
- `security/`: relocation, tampering, bypass, timing, and attack-oriented tests

The final test plan should cover both:

- embedded-policy and executable-image integrity checks
- authorization behavior after successful verification
