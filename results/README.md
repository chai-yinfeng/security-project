# Results

- `raw/`: generated experiment outputs
- `processed/`: summarized tables, plots, and report-ready data

Raw result files may become large. Keep enough metadata to reproduce every processed result.

